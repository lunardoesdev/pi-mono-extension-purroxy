{
  "name": "pi-mono-extension-purroxy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "clean": "echo 'nothing to clean'",
    "build": "echo 'nothing to build'",
    "check": "echo 'use: npx tsc --noEmit src/index.ts --module nodenext --moduleResolution nodenext --target es2022 --skipLibCheck'"
  },
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "dependencies": {
    "@google/genai": "^1.40.0",
    "@mariozechner/pi-ai": "^0.66.1",
    "@mariozechner/pi-coding-agent": "^0.66.1"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "typescript": "^5.9.3"
  }
}
